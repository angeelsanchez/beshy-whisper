'use client';

import FAQ from '@/components/FAQ';

export default function FAQSection() {
  return (
    <section id="faq" aria-label="Preguntas Frecuentes" className="py-20 bg-app-bg text-app-text">
      <FAQ />
    </section>
  );
}
